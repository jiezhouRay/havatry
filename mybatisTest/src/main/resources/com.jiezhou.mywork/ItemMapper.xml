<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jiezhou.mywork.mapper.ItemMapper">
    <select id ="getItermMessage" parameterType="String" resultType="com.jiezhou.mywork.entity.Item" >
        select * from iterm where code =#{code}
    </select>

</mapper>

